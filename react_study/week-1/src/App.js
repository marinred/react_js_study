// import "./App.css";
import "App.css"; // 절대경로 설정후... root/jsconfig.json
// ./ 상대경로

// 이 파일이 우리의 PlayGround!
function App() {
  return (
    <div className="App">
      {/* 여기에서 코드 작성 */}
      <p>Start React</p>
    </div>
  );
}

export default App;

// 절대경로 지정(root 경로)
